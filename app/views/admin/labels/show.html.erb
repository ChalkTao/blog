<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @label.name %>
</p>

<%= link_to t('.back', :default => t("helpers.links.back")), edit_label_path(@label) %> |
<%= link_to t('.back', :default => t("helpers.links.back")), labels_path %>
