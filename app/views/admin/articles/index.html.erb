<%- model_class = Article -%>
<div class="page-header">
<h1><%= t("admin.navbar.articles") %></h1>
</div>

<%= render "table" %>

<%= link_to t('.new', :default => t("helpers.links.new")),
            new_admin_article_path,
            :class => 'btn btn-primary' %>
