<div class="well sidebar-nav">
  <ul class="nav nav-list">
  	<li class="nav-header"><%= t("admin.sider.article") %></li>
  	<li><%= link_to t("admin.sider.new_article"), new_admin_article_path %></li>
    <li class="nav-header"><%= t("admin.sider.labels") %></li>
    <% @side_label.each do |label| %>
    <li><%= link_to label.name, admin_label_path(label) %></li>
    <% end %>
  </ul>
</div>
