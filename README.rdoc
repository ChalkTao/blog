== README

Blog build on RoR, using bootstrap and mongoid

=== USAGE

* install dependency: <tt>bundle install</tt>

* start localhost mongo server: <tt>mongod &</tt>, make sure you have mongod installed

* start server: <tt>rails s</tt>

* view website on http://localhost:3000

